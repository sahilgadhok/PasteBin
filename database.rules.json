{
  "rules": {
    "file": {
      ".read": "!data.child('uid').exists() || (auth != null && data.child('uid').val() == auth.uid)",
      ".write": "!newData.child('uid').exists() || (auth != null && newData.child('uid').val() == auth.uid)"
    }
  }
}
